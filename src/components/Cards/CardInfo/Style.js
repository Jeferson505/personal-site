import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    card: {
        marginLeft: 50,
    },
    title: {
        fontSize: 14,
    },
    description: {
        textAlign: "justify",
        marginBottom: 10,
    }
});
